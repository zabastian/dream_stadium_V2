services:
  mysql:
    image: mysql:latest
    container_name: dream-stadium-V2
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: dream_stadium_V2
    ports:
      - "3306:3306"

  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - mysql